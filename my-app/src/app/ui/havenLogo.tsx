import { playwrite } from '@/app/ui/fonts/fonts';

export default function HavenLogo() {
  return (
    <h1
      className={`${playwrite.className} text-[#F2E9E4] hover:underline p-2 w-25`}
    >
      HandCrafted Haven
    </h1>
  );
}
