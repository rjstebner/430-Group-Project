import { Playwrite_US_Trad, Roboto } from "next/font/google";

export const playwrite_vn = Playwrite_US_Trad({
  weight: '300',
});

export const roboto = Roboto({
  subsets: ['latin'],
  weight: '400',
  style: 'normal',
});
